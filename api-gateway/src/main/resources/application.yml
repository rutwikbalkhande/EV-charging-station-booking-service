server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: test-service
          uri: lb://Station-service
          predicates:
            - Path=/station/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka